{% extends "layout.html.twig" %}

{% block title %}New Email{% endblock %}

{% block content %}
	<div class="container">
	<div class="row">
		<div class="col-lg-12">
			<h1 class="page-header">New Email
			</h1>
			<ol class="breadcrumb">
				<li><a href="/">Home</a></li>
				<li><a href="et-helper">ExactTarget API Helper</a></li>
				<li class="active">New Email</li>
			</ol>
		</div>
	</div>


	{{ form_start(emailForm,  { 'attr': {'class': 'form-horizontal'} })}}
		{{form_row(emailForm.Name, {'label': 'Name'}) }}
		{{form_row(emailForm.Subject, {'label': 'Subject'}) }}
		{{form_row(emailForm.Description, {'label': 'Description'}) }}
		{{form_row(emailForm.CustomerKey, {'label' : 'Customer Key'}) }}
		{{ form_row(emailForm.Subject, {'label' : 'Subject'}) }}
		{{ form_row(emailForm.HTMLBody, {'label': 'Body'}) }}
		{{ form_row(emailForm.Category, {'label' : 'Category'})}}
		 <input type="submit" name="submit" value="Create Email" />
	{{ form_end(emailForm) }}
   
</form>
	</div>
{% endblock %}