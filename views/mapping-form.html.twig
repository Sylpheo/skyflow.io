{% extends "layout.html.twig" %}

{% block title %}New mapping{% endblock %}

{% block content %}
<div class="container">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">New Mapping
            </h1>
            <ol class="breadcrumb">
                <li><a href="/">Home</a></li>
                <li><a href="mapping">Mapping</a></li>
                <li class="active">Create</li>
            </ol>
        </div>
    </div>


        {{form_start(mappingForm,  { 'attr': {'class': 'form-horizontal'} })}}
        {{form_row(mappingForm.event, {'label' : 'Event'}) }}
        {{form_row(mappingForm.flow, {'label' : 'Flow'}) }}
        <input type="submit" name="submit" value="Add !" />
        {{form_end(mappingForm)}}

</div>
{% endblock %}
