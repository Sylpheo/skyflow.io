{% extends "layout.html.twig" %}

{% block title %}Add Event/Trigger{% endblock %}

{% block content %}

<form action="#" method="post">
	{{form_start(eventForm)}}
		{{form_row(eventForm.event, {'label' : 'Event'}) }}
		{{form_row(eventForm.trigger, {'label' : 'Trigger'}) }}

	
	 <input type="submit" name="submit" value="Add !" />
   {{form_end(eventForm)}}
</form>
{% endblock %}
